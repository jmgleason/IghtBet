import React from 'react';
import { Text } from 'react-native';

const ActiveBetsScreen = props => {
  return (
    <Text>This is showing just accepted bets</Text>
  );
};

export default ActiveBetsScreen;